#ifndef HASH_H
#define HASH_H

#include <iostream>
#include <iomanip>
#include <string>
using namespace std;
std::dec;

size_t hash(size_t x);
size_t hash(string str);
void toHex(size_t x);



#endif
