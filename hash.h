#ifndef HASH_H
#define HASH_H

#include <iostream>
#include <iomanip>
#include <string>
using namespace std;


size_t hash(size_t x);
size_t hash(string str);
size_t hash2(string str);
void toHex(size_t x);
void toHex(string str);



#endif
